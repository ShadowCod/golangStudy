package main

// map排序：没有默认的排序方法
func main() {
	// mapNum := make(map[int]int, 5)
	// mapNum[10] = 100
	// mapNum[1] = 13
	// mapNum[4] = 5
	// mapNum[8] = 89
	// fmt.Println(mapNum)
	// var slice []int
	// for k, _ := range mapNum {
	// 	slice = append(slice, k)
	// }
	// fmt.Println(slice)
	// sort.Ints(slice)
	// fmt.Println(slice)
	// for _, v := range slice {
	// 	fmt.Printf("key:%v value:%v\n", v, mapNum[v])
	// }

	// 使用细节
	// 1) map是引用类型
	// 2) map是可以自动增长的
	// 3) map中的value中使用struct更易管理
}
